# Public assets

- **`favicon.svg`** — App icon (referenced from `index.html`).
- **`test-schema/`** — Minimal id-tagging-schema–shaped JSON used by Playwright e2e tests (`bun run test:e2e`). Not required at runtime for normal use; safe to remove only if you drop or rewrite those tests.
