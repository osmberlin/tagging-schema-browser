# Intentional omissions of slash-parent `fields` / `moreFields` inheritance.
#
# Add an entry when a preset deliberately defines an explicit field list without
# `{parent}` and without inheriting some parent fields. Record `missedFieldIds`
# as a snapshot — CI fails when the live schema no longer matches (stale override).
#
# `fields` and `moreFields` are independent: you may record only one list when
# the other still inherits via `{parent}` or remains unreviewed. Omitting a list
# key is valid; extra keys with no live detection are stale.
#
# Entries may also be proposed from the Tagging Schema Browser (GitHub issues with
# a Snapshot YAML block). See `.agents/skills/apply-schema-override/SKILL.md`.
#
# `fields` and `moreFields` are independent: you may record only one list when
# the other still inherits via `{parent}` or remains unreviewed. Omitting a list
# key is valid; extra keys with no live detection are stale.
#
# CI validates against the published npm release dist (see validate-inheritance-overrides).
#
# Example from id-tagging-schema: tourism/information/terminal.json lists only
# `operator` in fields (not `{tourism/information}`) while moreFields inherits
# from the parent via `{tourism/information}`.
#
#   tourism/information/terminal:
#     fields:
#       parentId: tourism/information
#       missedFieldIds:
#         - address
#         - building_area_yes
version: 1
presets:
  aerialway/pylon:
    fields:
      parentId: aerialway
      missedFieldIds:
        - name
  amenity/shelter/lean_to:
    fields:
      parentId: amenity/shelter
      missedFieldIds:
        - bench
        - bin
  barrier/jersey_barrier:
    moreFields:
      parentId: barrier
      missedFieldIds:
        - level
  barrier/swing_gate:
    moreFields:
      parentId: barrier
      missedFieldIds:
        - level
  building/construction:
    fields:
      parentId: building
      missedFieldIds:
        - building/levels
        - height
  education/college:
    fields:
      parentId: education
      missedFieldIds:
        - building_area
    moreFields:
      parentId: education
      missedFieldIds:
        - branch_brand
        - brand
        - building/levels_building
        - height_building
  highway/footway/access_aisle:
    fields:
      parentId: highway/footway
      missedFieldIds:
        - name
        - tunnel/name
    moreFields:
      parentId: highway/footway
      missedFieldIds:
        - bridge/name
        - bridge/ref
        - informal
        - not/name
        - oneway
  highway/give_way:
    fields:
      parentId: highway
      missedFieldIds:
        - name
  highway/speed_camera:
    fields:
      parentId: highway
      missedFieldIds:
        - name
  highway/stop:
    fields:
      parentId: highway
      missedFieldIds:
        - name
  highway/street_lamp:
    fields:
      parentId: highway
      missedFieldIds:
        - name
  highway/toll_gantry:
    fields:
      parentId: highway
      missedFieldIds:
        - name
  historic/castle:
    fields:
      parentId: historic
      missedFieldIds:
        - inscription
  historic/city_gate:
    fields:
      parentId: historic
      missedFieldIds:
        - inscription
  historic/memorial/blue_plaque-GB-IE:
    fields:
      parentId: historic/memorial
      missedFieldIds:
        - material
    moreFields:
      parentId: historic/memorial
      missedFieldIds:
        - gnis/feature_id-US
  leisure/fitness_station:
    fields:
      parentId: leisure
      missedFieldIds:
        - name
  man_made/adit:
    moreFields:
      parentId: man_made
      missedFieldIds:
        - material
  man_made/antenna:
    fields:
      parentId: man_made
      missedFieldIds:
        - name
  man_made/beehive:
    fields:
      parentId: man_made
      missedFieldIds:
        - name
  man_made/breakwater:
    fields:
      parentId: man_made
      missedFieldIds:
        - name
  man_made/cairn:
    fields:
      parentId: man_made
      missedFieldIds:
        - name
  man_made/carpet_hanger:
    fields:
      parentId: man_made
      missedFieldIds:
        - name
  man_made/manhole:
    fields:
      parentId: man_made
      missedFieldIds:
        - name
  man_made/petroleum_well:
    moreFields:
      parentId: man_made
      missedFieldIds:
        - material
  man_made/reservoir_covered:
    moreFields:
      parentId: man_made
      missedFieldIds:
        - material
  man_made/silo:
    fields:
      parentId: man_made
      missedFieldIds:
        - name
  man_made/storage_tank:
    fields:
      parentId: man_made
      missedFieldIds:
        - name
  man_made/street_cabinet:
    fields:
      parentId: man_made
      missedFieldIds:
        - name
  man_made/surveillance:
    fields:
      parentId: man_made
      missedFieldIds:
        - name
  man_made/tower/cooling:
    fields:
      parentId: man_made/tower
      missedFieldIds:
        - tower/construction
    moreFields:
      parentId: man_made/tower
      missedFieldIds:
        - architect
        - gnis/feature_id-US
  man_made/video_wall:
    fields:
      parentId: man_made
      missedFieldIds:
        - name
    moreFields:
      parentId: man_made
      missedFieldIds:
        - material
  man_made/water_works:
    moreFields:
      parentId: man_made
      missedFieldIds:
        - material
  natural/water/stream:
    fields:
      parentId: natural/water
      missedFieldIds:
        - name
  power/plant/source/solar/photovoltaic:
    fields:
      parentId: power/plant/source/solar
      missedFieldIds:
        - plant/output
  tourism/information/terminal:
    fields:
      parentId: tourism/information
      missedFieldIds:
        - address
        - building_area_yes
  type/route/aerialway:
    fields:
      parentId: type/route
      missedFieldIds:
        - network/type
  type/route/bus:
    fields:
      parentId: type/route
      missedFieldIds:
        - network/type
  waterway/ditch:
    fields:
      parentId: waterway
      missedFieldIds:
        - name
