# Intentional omissions of slash-parent `fields` / `moreFields` inheritance.
#
# Add an entry when a preset deliberately defines an explicit field list without
# `{parent}` and without inheriting some parent fields. Record `missedFieldIds`
# as a snapshot — CI fails when the live schema no longer matches (stale override).
#
# CI validates against the published npm release dist (see validate-inheritance-overrides).
#
# Example from id-tagging-schema: tourism/information/terminal.json lists only
# `operator` in fields (not `{tourism/information}`) while moreFields inherits
# from the parent via `{tourism/information}`.
#
#   tourism/information/terminal:
#     fields:
#       parentId: tourism/information
#       missedFieldIds:
#         - address
#         - building_area_yes
version: 1
presets:
  tourism/information/terminal:
    fields:
      parentId: tourism/information
      missedFieldIds:
        - address
        - building_area_yes
  man_made/adit:
    moreFields:
      parentId: man_made
      missedFieldIds:
        - material
  man_made/antenna:
    fields:
      parentId: man_made
      missedFieldIds:
        - name
  man_made/breakwater:
    fields:
      parentId: man_made
      missedFieldIds:
        - name
  man_made/cairn:
    fields:
      parentId: man_made
      missedFieldIds:
        - name
  aerialway/pylon:
    fields:
      parentId: aerialway
      missedFieldIds:
        - name
  type/route/aerialway:
    fields:
      parentId: type/route
      missedFieldIds:
        - network/type
  type/route/bus:
    fields:
      parentId: type/route
      missedFieldIds:
        - network/type
  amenity/shelter/lean_to:
    fields:
      parentId: amenity/shelter
      missedFieldIds:
        - bench
        - bin
  barrier/jersey_barrier:
    moreFields:
      parentId: barrier
      missedFieldIds:
        - level
  man_made/manhole:
    fields:
      parentId: man_made
      missedFieldIds:
        - name
  leisure/fitness_station:
    fields:
      parentId: leisure
      missedFieldIds:
        - name
  highway/toll_gantry:
    fields:
      parentId: highway
      missedFieldIds:
        - name
  man_made/petroleum_well:
    moreFields:
      parentId: man_made
      missedFieldIds:
        - material
  man_made/reservoir_covered:
    moreFields:
      parentId: man_made
      missedFieldIds:
        - material
  man_made/silo:
    fields:
      parentId: man_made
      missedFieldIds:
        - name
    moreFields:
      parentId: man_made
      missedFieldIds:
        - material
  power/plant/source/solar/photovoltaic:
    fields:
      parentId: power/plant/source/solar
      missedFieldIds:
        - plant/output
  highway/speed_camera:
    fields:
      parentId: highway
      missedFieldIds:
        - name
  highway/stop:
    fields:
      parentId: highway
      missedFieldIds:
        - name
  man_made/storage_tank:
    fields:
      parentId: man_made
      missedFieldIds:
        - name
  natural/water/stream:
    fields:
      parentId: natural/water
      missedFieldIds:
        - name
  man_made/surveillance:
    fields:
      parentId: man_made
      missedFieldIds:
        - name
  highway/street_lamp:
    fields:
      parentId: highway
      missedFieldIds:
        - name
  man_made/street_cabinet:
    fields:
      parentId: man_made
      missedFieldIds:
        - name
    moreFields:
      parentId: man_made
      missedFieldIds:
        - material
  man_made/video_wall:
    fields:
      parentId: man_made
      missedFieldIds:
        - name
    moreFields:
      parentId: man_made
      missedFieldIds:
        - material
  barrier/swing_gate:
    moreFields:
      parentId: barrier
      missedFieldIds:
        - level
  education/college:
    fields:
      parentId: education
      missedFieldIds:
        - building_area
    moreFields:
      parentId: education
      missedFieldIds:
        - branch_brand
        - brand
        - building/levels_building
        - height_building
  waterway/ditch:
    fields:
      parentId: waterway
      missedFieldIds:
        - name
  historic/castle:
    fields:
      parentId: historic
      missedFieldIds:
        - inscription
