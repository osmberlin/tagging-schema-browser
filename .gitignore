# Dependencies & build
node_modules
dist
tsconfig.tsbuildinfo
.tsbuildinfo-*
*.local
.env
.DS_Store

# Local editor / agent tooling
.claude

# Reference-only (large upstream UI kits)
.tailwind-component-source

# Playwright / tests
test-results
playwright-report
playwright/.cache
package-lock.json
