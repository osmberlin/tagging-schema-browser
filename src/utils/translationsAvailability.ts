/** Staging/interim dist ships English only; npm releases include community translations. */
export const TRANSLATIONS_RELEASE_ONLY_MESSAGE =
  'Translation comparison is only available for the published release or a pinned release dist URL (?dataUrl=…@version/dist).'
